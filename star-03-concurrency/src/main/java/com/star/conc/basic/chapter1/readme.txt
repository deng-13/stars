1. 类 semaphore 的主要作用是限制并发执行的线程个数，它具有 synchronized 所不具备的强大功能：
  1> 等待获取许可的同时可以假如等待时间
  2> 尝试是否可以持有锁等这类扩展功能

  --semaphore 是强有力的控制并发线程个数的解决方案之一
2. exchanger 是线程间传输数据的方式之一，并且在传输的数据类型上并没有任何限制
  1> 具有阻塞特性
  2> 交换数据
  3> 在线程时间交换的数据 必须是同类型的